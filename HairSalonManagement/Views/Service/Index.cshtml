@{
    ViewData["Title"] = "Hizmetlerimiz";
}

@model IEnumerable<HairSalonManagement.Models.Service>


    <a asp-action="HizmetEkle">Yeni Hizmet Ekle</a>

<div class="alert-danger"> @TempData["msj"]</div>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Price)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DurationMinutes)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
//@foreach (var item in Model) { 
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Price)
            </td>
            <td>
                 @Html.DisplayFor(modelItem => item.DurationMinutes)
            </td>
            <td>
                    <a asp-action="HizmetDüzenle" asp-route-id="@item.ServiceID" class="btn btn-primary">Hizmet Düzenle</a> |
                    <a asp-action="HizmetDetay" asp-route-id="@item.ServiceID" class="btn btn-primary">Hizmet Detay</a> |
                    <a asp-action="HizmetSil" asp-route-id="@item.ServiceID" class="btn btn-primary">Hizmet Sil</a>
                    <a asp-action="ServiceDetails" asp-route-serviceId="@item.ServiceID" class="btn btn-primary">Çalışan Ekle</a>

            </td>
        </tr>
}
    </tbody>
</table>


